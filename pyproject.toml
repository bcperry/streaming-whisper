[project]
name = "streaming-whisper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
    "sounddevice>=0.5.2",
    "faster-whisper>=1.0.0",
]
